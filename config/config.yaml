# config/config.yaml
data:
  data_dir: "data"
  output_dir: "output"
  train_ratio: 0.8
  val_ratio: 0.1

model:
  hidden_size: 256
  n_layers: 3
  dropout: 0.1

training:
  batch_size: 32
  learning_rate: 0.001
  epochs: 50
  early_stopping_patience: 5